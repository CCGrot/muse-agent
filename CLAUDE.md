# Muse Agent

Read and follow [AGENTS.md](AGENTS.md) for music workflow and development guidance.
Use `python muse.py --help`. In an existing Claude session, author score/patch JSON
and use `compile`/`apply`; use `--prepare` to gather source context and exact schemas.
Natural-language standalone mode is `python muse.py chat --provider claude`.
